pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Dariar-Danire/testJenkins.git', branch: 'prod'
            }
        }
        
        stage('Checking for file availability') {
            steps {
                script {
                    if (fileExists('prod.go')) {
                        echo "File prod.go found"
                    } else {
                        error "File prod.go not found"
                    }
                }
            }
        }
    }
    post{
        success{
            echo 'You can go home'
        }
        failure{
            echo 'Sit and work on'
        }
    }
}
